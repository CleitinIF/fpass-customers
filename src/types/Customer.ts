export default interface Customer {
  [key: string]: string;
  id: string;
  name: string;
  birthday: string;
  cellphone: string;
  document: string;
  email: string;
  address: string;
  observation: string;
}